export declare type FormatMode = "fast" | "full";
declare type SN = string | number;
export declare type FormatValidator<T extends SN> = (data: T) => boolean;
export declare type FormatCompare<T extends SN> = (data1: T, data2: T) => boolean;
export declare type AsyncFormatValidator<T extends SN> = (data: T) => PromiseLike<boolean>;
export interface FormatDefinition<T extends SN> {
    type: T extends string ? "string" : "number";
    validate: FormatValidator<T> | (T extends string ? string | RegExp : never);
    async?: false | undefined;
    compare?: FormatCompare<T>;
}
export interface AsyncFormatDefinition<T extends SN> {
    type: T extends string ? "string" : "number";
    validate: AsyncFormatValidator<T>;
    async: true;
    compare?: FormatCompare<T>;
}
export declare type Format = string | RegExp | FormatValidator<string> | FormatDefinition<any> | AsyncFormatDefinition<any>;
export declare type FormatName = "date" | "time" | "date-time" | "uri" | "uri-reference" | "uri-template" | "url" | "email" | "hostname" | "ipv4" | "ipv6" | "regex" | "uuid" | "json-pointer" | "json-pointer-uri-fragment" | "relative-json-pointer";
export declare type DefinedFormats = {
    [key in FormatName]: Format;
};
export declare const formats: {
    fast: DefinedFormats;
    full: DefinedFormats;
};
export {};
